@{
    Layout = "_EditorLayout";
}
@model List<TvSerie>;
    <h2>Lista Serie</h2>
    <a href="@Url.Action("Create", "Serie")" class="btn btn-secondary">Nuova +</a>
    @if(Model.Count == 0){
        <div class="alert container" role="alert">
            Non sono presenti Seie tv nella tua lista
        </div>
    }
    else{
        @*<div class="container-fluid debug">
            <div class="row">
                <div class="col-12 col-md-6 debug"></div>
                <div class="col-12 col-md-6 debug"></div>

            </div>
        </div>*@
        <table class="table">
          <thead>
            <tr>
              <th scope="col">#Id</th>
              <th scope="col">Nome</th>
              <th scope="col">Azioni</th>
            </tr>
          </thead>
          <tbody>
              @foreach(TvSerie serie in Model){

                <tr>
                    <th scope="row">@serie.Id</th>
                    <td>@serie.Title</td>
                    <td>
                        <a href="@Url.Action("Update", "Serie", new{Id = serie.Id})">Modifica</a>
                        <form asp-controller="Serie" asp-action="Delete" asp-route-id="@serie.Id" method="post" class="d-inline">
                        @Html.AntiForgeryToken()
                            <input type="submit" class="btn rounded-pill" value="Elimina">
                        </form>
                    </td>
                </tr>
              }
          </tbody>
        </table>

    }
